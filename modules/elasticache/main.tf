# Skeleton module for ElastiCache clusters/replication groups

# Example: redis replication group
# resource "aws_elasticache_replication_group" "this" {
#   replication_group_id = var.name
#   node_type            = var.node_type
# }
