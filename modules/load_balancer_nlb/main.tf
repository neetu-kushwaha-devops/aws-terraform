# Skeleton module for Network Load Balancer (NLB)

# Example:
# resource "aws_lb" "this" {
#   name               = var.name
#   internal           = false
#   load_balancer_type = "network"
#   subnets            = var.subnets
#   tags               = var.tags
# }
