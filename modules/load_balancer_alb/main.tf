# Skeleton module for Application Load Balancer (ALB)

# Example:
# resource "aws_lb" "this" {
#   name               = var.name
#   internal           = false
#   load_balancer_type = "application"
#   subnets            = var.subnets
#   tags               = var.tags
# }
