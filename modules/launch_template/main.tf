# Skeleton module for Launch Template

# Example:
# resource "aws_launch_template" "this" {
#   name_prefix   = var.name
#   image_id      = var.ami
#   instance_type = var.instance_type
#   tag_specifications { resource_type = "instance" tags = var.tags }
# }
